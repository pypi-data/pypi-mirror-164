# https://black.readthedocs.io/en/stable/
[tool.black]
line-length = 79
target-version = ['py310']
extend-exclude = "(.tox|.eggs|build|dist|typing|.temp|external)"

[build-system]
requires = ["setuptools", "wheel"]
