from setuptools import setup

setup(name='vin2proxy',
      version='0.0',
      description='vin to vin proxy conversion',
      packages=['vin2proxy'],
      author_email='amankumar.gupta@maruti.co.in',
      zip_safe=False)