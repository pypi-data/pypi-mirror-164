[tool.poetry]
name = "tfparse"
version = "0.1.0"
description = "Terraform HCL Parser"
authors = ["Stacklet <info@stacklet.io>"]
license = "Apache-2"

[tool.poetry.dependencies]
python = "^3.8"

[tool.poetry.dev-dependencies]

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
