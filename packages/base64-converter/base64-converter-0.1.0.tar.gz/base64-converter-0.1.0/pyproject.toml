[tool.poetry]
name = "base64-converter"
version = "0.1.0"
description = ""
authors = ["johnyacuta <example@gmail.com>"]
readme = "README.md"

[tool.poetry.scripts]
base64-converter = "base64_converter.main:app"

[tool.poetry.dependencies]
python = "^3.10"
typer = {extras = ["all"], version = "^0.6.1"}

[tool.poetry.dev-dependencies]
pytest = "^5.2"

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
