from setuptools import setup

setup(name='ah-distributions-gauss-binom',
      version='0.1',
      description='Gaussian distributions',
      packages=['ah-distributions-gauss-binom'],
      zip_safe=False)
