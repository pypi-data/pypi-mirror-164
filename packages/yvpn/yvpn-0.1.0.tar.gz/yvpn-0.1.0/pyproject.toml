[tool.poetry]
name = "yvpn"
version = "0.1.0"
description = ""
authors = ["Ben Simcox <ben@bnsmcx.com>"]
readme = "README.md"

[tool.poetry.scripts]
yvpn = "yvpn.main:main"

[tool.poetry.dependencies]
python = "^3.10"
typer = {extras = ["all"], version = "^0.6.1"}
requests = "^2.28.1"
paramiko = "^2.11.0"

[tool.poetry.dev-dependencies]
pytest = "^5.2"

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
